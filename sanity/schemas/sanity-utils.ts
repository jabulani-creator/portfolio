/**
 * @deprecated Import from `@/lib/content/queries/*` for new code.
 * This file re-exports legacy portfolio queries so existing pages keep working.
 */
export {
  getProjects,
  getProject,
  getServices,
  getSkills,
} from "../../src/lib/content/queries/legacy";
